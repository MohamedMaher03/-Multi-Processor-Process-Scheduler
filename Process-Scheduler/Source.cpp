#pragma once
#include <iostream>
#include"PROCESS.h"
#include"PROCESSOR.h"
#include"UI.h"
#include"Scheduler.h"
#include"LinkedQueue.h"
#include"LinkedPriorityQueue.h"
using namespace std;

int main()
{
	//test
	/*
	LinkedQueue<int>q1;
	q1.enqueue(5);
	q1.enqueue(9);
	q1.enqueue(4);

	while (!q1.isEmpty()) {
		int x;
		q1.dequeue(x);
		cout << x << " ";
	}
	cout << endl;
	LinkedPriorityQueue<int>q2;
	q2.enqueue(5);
	q2.enqueue(9);
	q2.enqueue(4);

	while (!q2.isEmpty()) {
		int y;
		q2.dequeue(y);
		cout << y << " ";
	}
	return 0;
	*/
}